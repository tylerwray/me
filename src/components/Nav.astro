---
import NavItem from "./NavItem.astro";
import ThemeButton from "./ThemeButton.astro";
const emoji = ["🙈", "🌀", "💻", "🤘🏻"];
const icon = emoji[Math.floor(Math.random() * emoji.length)];
console.log("BUILDING PAGE pathname -> ", Astro.url.pathname);
---

<nav
  class="grid grid-cols-[max-content,auto,max-content,max-content,max-content,max-content] p-8 max-w-3xl mx-auto items-center w-full"
>
  <a href="/" class="text-black dark:text-white no-underline">
    <span class="text-2xl" role="img" aria-label="emoji">{icon}</span>
    <span class="ml-4 text-2xl hidden sm:inline font-mono"> Tyler Wray</span>
  </a>
  <div></div>
  <NavItem to="/" isCurrent={Astro.url.pathname === "/"}>Posts</NavItem>
  <NavItem to="/me" isCurrent={Astro.url.pathname === "/me"}>Me</NavItem>
  <NavItem to="/contact" isCurrent={Astro.url.pathname === "/contact"}>Contact</NavItem>
  <ThemeButton />
</nav>
